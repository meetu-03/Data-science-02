# Task 5: Multi-API Aggregator (Weather & Crypto)

# Question: Combine data from two APIs: fetch the current temperature in Mumbai from OpenWeatherMap and the latest Bitcoin price from CoinGecko, then display both results together in a single output. Constraint: Handle errors gracefully if either API call fails and show an appropriate message.